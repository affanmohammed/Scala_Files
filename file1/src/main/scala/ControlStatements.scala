object ControlStatements
{
  def main(args: Array[String]): Unit =
  {
    import scala.io.StdIn
/*
    // if else:-
    println("Enter a number: ")
    var x1 = StdIn.readLine().toInt  //typecast to integer datatype

    if (x1>0){
      println("Value is positive")
    } else if (x1<0){
      println("value is negative")
    } else {
      println("value is zero")
    }

    val x2 = StdIn.readLine().toInt
    val output =if (x2 % 2==0) "even" else "odd"
    println(output)

    var i=1
    while (i<=5)
      {
        println(s"Value of i is: $i")
        i += 1
      }


    val List1 = List(2,4,6,"abc",9,1,"bfr",15,17,20)
    for (i<-List1) {
      println(i)
    }


  val nums = (1 to 50).toList
    for (i <- nums) {
      if (i%2==0)
        {
        println(i)
        }
      }

    val nums = (1 to 50).toList
    val even = for {
        i <- nums
        if i % 2 == 0
      } yield i
    println(even)


// prime
    val prime  = for{
      i <- 2 to 100
      if (2 until i).forall(x => i%x !=0)
    } yield i
    println(prime)

*/


  }
}